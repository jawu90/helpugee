
class ContextKey {
    public readonly username = 'username';
    public readonly sectionId = 'sectionId';
    public readonly role = 'role';
}

const contextKey: ContextKey = new ContextKey();

export default contextKey;